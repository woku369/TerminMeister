{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "Set-Location 'C:\\Users\\wolfg\\Desktop\\TerminMeister'; if (git status --porcelain) { git add -A; git commit -m \"auto: $(Get-Date -Format 'yyyy-MM-dd HH:mm')\" --quiet; git push origin main --quiet }",
            "timeout": 60,
            "async": true
          }
        ]
      }
    ]
  }
}
